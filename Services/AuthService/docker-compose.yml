services:
  authservice:
    build: .
    ports:
      - "8000:8000"
    environment:
      ConnectionStrings__DefaultConnection: "Host=host.docker.internal;Port=5432;Database=egeeks_auth;Username=postgres;Password=root"
