services:
  storeservice:
    build: .
    ports:
      - "8080:8080"
    environment:
      ConnectionStrings__DefaultConnection: "Host=host.docker.internal;Port=5432;Database=egeeks_store;Username=postgres;Password=root"
